package dev.sopho.fdx.client

import kotlin.test.Test
import kotlin.test.assertTrue

/** 占位冒烟测试：验证 JVM 测试脚手架能跑起来（依赖、testOptions、JUnit 运行器都就位）。 */
class SmokeTest {
    @Test
    fun `scaffold_runs`() {
        assertTrue(true, "JVM 测试脚手架就位")
    }
}
